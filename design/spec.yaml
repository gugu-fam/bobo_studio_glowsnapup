files:
  - file: lib/features/product/photo_studio.dart
    classes:
      - name: PhotoStudioService
        methods:
          - name: processImage
            args:
              - name: image
                type: File
            return: Future<StudioResult>
            exceptions: []
            responsibilities: "画像を処理してスタジオ効果を適用する"
state_transitions:
  PhotoStudioState:
    - Idle
    - Loading
    - Success
    - Error
